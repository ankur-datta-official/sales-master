generator client {
  provider        = "prisma-client"
  output          = "../src/generated/prisma"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
  schemas  = ["auth", "public"]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model custom_oauth_providers {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider_type         String
  identifier            String    @unique
  name                  String
  client_id             String
  client_secret         String
  acceptable_client_ids String[]  @default([])
  scopes                String[]  @default([])
  pkce_enabled          Boolean   @default(true)
  attribute_mapping     Json      @default("{}")
  authorization_params  Json      @default("{}")
  enabled               Boolean   @default(true)
  email_optional        Boolean   @default(false)
  issuer                String?
  discovery_url         String?
  skip_nonce_check      Boolean   @default(false)
  cached_discovery      Json?
  discovery_cached_at   DateTime? @db.Timestamptz(6)
  authorization_url     String?
  token_url             String?
  userinfo_url          String?
  jwks_uri              String?
  created_at            DateTime  @default(now()) @db.Timestamptz(6)
  updated_at            DateTime  @default(now()) @db.Timestamptz(6)

  @@index([created_at])
  @@index([enabled])
  @@index([identifier])
  @@index([provider_type])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                 @id @db.Uuid
  user_id                String?                @db.Uuid
  auth_code              String?
  code_challenge_method  code_challenge_method?
  code_challenge         String?
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?              @db.Timestamptz(6)
  updated_at             DateTime?              @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?              @db.Timestamptz(6)
  invite_token           String?
  referrer               String?
  oauth_client_state_id  String?                @db.Uuid
  linking_target_id      String?                @db.Uuid
  email_optional         Boolean                @default(false)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String    @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime? @db.Timestamptz(6)
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
  email           String?   @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String           @id @db.Uuid
  user_id                      String           @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime         @db.Timestamptz(6)
  updated_at                   DateTime         @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?        @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?          @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([friendly_name, user_id], map: "mfa_factors_user_friendly_name_unique", where: raw("(TRIM(BOTH FROM friendly_name) <> ''::text)"))
  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 users?                     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at], map: "oauth_auth_pending_exp_idx", where: raw("(status = 'pending'::auth.oauth_authorization_status)"))
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                         String                  @id @db.Uuid
  client_secret_hash         String?
  registration_type          oauth_registration_type
  redirect_uris              String
  grant_types                String
  client_name                String?
  client_uri                 String?
  logo_uri                   String?
  created_at                 DateTime                @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at                 DateTime?               @db.Timestamptz(6)
  client_type                oauth_client_type       @default(confidential)
  token_endpoint_auth_method String
  oauth_authorizations       oauth_authorizations[]
  oauth_consents             oauth_consents[]
  sessions                   sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([client_id], map: "oauth_consents_active_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, client_id], map: "oauth_consents_active_user_client_idx", where: raw("(revoked_at IS NULL)"))
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  users      users               @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([oauth_client_id])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model users {
  instance_id                 String?                @db.Uuid
  id                          String                 @id @db.Uuid
  aud                         String?                @db.VarChar(255)
  role                        String?                @db.VarChar(255)
  email                       String?                @unique(map: "users_email_partial_key", where: raw("(is_sso_user = false)")) @db.VarChar(255)
  encrypted_password          String?                @db.VarChar(255)
  email_confirmed_at          DateTime?              @db.Timestamptz(6)
  invited_at                  DateTime?              @db.Timestamptz(6)
  confirmation_token          String?                @unique(map: "confirmation_token_idx", where: raw("((confirmation_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  confirmation_sent_at        DateTime?              @db.Timestamptz(6)
  recovery_token              String?                @unique(map: "recovery_token_idx", where: raw("((recovery_token)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  recovery_sent_at            DateTime?              @db.Timestamptz(6)
  email_change_token_new      String?                @unique(map: "email_change_token_new_idx", where: raw("((email_change_token_new)::text !~ '^[0-9 ]*$'::text)")) @db.VarChar(255)
  email_change                String?                @db.VarChar(255)
  email_change_sent_at        DateTime?              @db.Timestamptz(6)
  last_sign_in_at             DateTime?              @db.Timestamptz(6)
  raw_app_meta_data           Json?
  raw_user_meta_data          Json?
  is_super_admin              Boolean?
  created_at                  DateTime?              @db.Timestamptz(6)
  updated_at                  DateTime?              @db.Timestamptz(6)
  phone                       String?                @unique
  phone_confirmed_at          DateTime?              @db.Timestamptz(6)
  phone_change                String?                @default("")
  phone_change_token          String?                @default("") @db.VarChar(255)
  phone_change_sent_at        DateTime?              @db.Timestamptz(6)
  confirmed_at                DateTime?              @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current  String?                @unique(map: "email_change_token_current_idx", where: raw("((email_change_token_current)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  email_change_confirm_status Int?                   @default(0) @db.SmallInt
  banned_until                DateTime?              @db.Timestamptz(6)
  reauthentication_token      String?                @unique(map: "reauthentication_token_idx", where: raw("((reauthentication_token)::text !~ '^[0-9 ]*$'::text)")) @default("") @db.VarChar(255)
  reauthentication_sent_at    DateTime?              @db.Timestamptz(6)
  is_sso_user                 Boolean                @default(false)
  deleted_at                  DateTime?              @db.Timestamptz(6)
  is_anonymous                Boolean                @default(false)
  identities                  identities[]
  mfa_factors                 mfa_factors[]
  oauth_authorizations        oauth_authorizations[]
  oauth_consents              oauth_consents[]
  one_time_tokens             one_time_tokens[]
  sessions                    sessions[]
  webauthn_challenges         webauthn_challenges[]
  webauthn_credentials        webauthn_credentials[]
  profiles                    profiles?

  @@index([created_at(sort: Desc)], map: "idx_users_created_at_desc")
  @@index([email], map: "idx_users_email")
  @@index([last_sign_in_at(sort: Desc)], map: "idx_users_last_sign_in_at_desc")
  @@index([instance_id])
  @@index([is_anonymous])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model webauthn_challenges {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String?  @db.Uuid
  challenge_type String
  session_data   Json
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  expires_at     DateTime @db.Timestamptz(6)
  users          users?   @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at])
  @@index([user_id])
  @@schema("auth")
}

model webauthn_credentials {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id          String    @db.Uuid
  credential_id    Bytes     @unique
  public_key       Bytes
  attestation_type String    @default("")
  aaguid           String?   @db.Uuid
  sign_count       BigInt    @default(0)
  transports       Json      @default("[]")
  backup_eligible  Boolean   @default(false)
  backed_up        Boolean   @default(false)
  friendly_name    String    @default("")
  created_at       DateTime  @default(now()) @db.Timestamptz(6)
  updated_at       DateTime  @default(now()) @db.Timestamptz(6)
  last_used_at     DateTime? @db.Timestamptz(6)
  users            users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model approval_logs {
  id                                                String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                                   String        @db.Uuid
  entity_type                                       String
  entity_id                                         String        @db.Uuid
  action                                            String
  from_user_id                                      String?       @db.Uuid
  to_user_id                                        String?       @db.Uuid
  acted_by_user_id                                  String        @db.Uuid
  note                                              String        @default("")
  created_at                                        DateTime      @default(now()) @db.Timestamptz(6)
  profiles_approval_logs_acted_by_user_idToprofiles profiles      @relation("approval_logs_acted_by_user_idToprofiles", fields: [acted_by_user_id], references: [id], onUpdate: NoAction)
  profiles_approval_logs_from_user_idToprofiles     profiles?     @relation("approval_logs_from_user_idToprofiles", fields: [from_user_id], references: [id], onUpdate: NoAction)
  organizations                                     organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles_approval_logs_to_user_idToprofiles       profiles?     @relation("approval_logs_to_user_idToprofiles", fields: [to_user_id], references: [id], onUpdate: NoAction)

  @@index([entity_type, entity_id], map: "approval_logs_entity_idx")
  @@index([organization_id, created_at(sort: Desc)], map: "approval_logs_org_created_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model attendance_sessions {
  id                String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id   String           @db.Uuid
  user_id           String           @unique(map: "attendance_sessions_one_open_per_user_idx", where: raw("(status = 'checked_in'::text)")) @db.Uuid
  check_in_at       DateTime         @default(now()) @db.Timestamptz(6)
  check_in_lat      Float?
  check_in_lng      Float?
  check_in_address  String?
  check_out_at      DateTime?        @db.Timestamptz(6)
  check_out_lat     Float?
  check_out_lng     Float?
  check_out_address String?
  status            String           @default("checked_in")
  device_info       String           @default("")
  created_at        DateTime         @default(now()) @db.Timestamptz(6)
  updated_at        DateTime         @default(now()) @db.Timestamptz(6)
  organizations     organizations    @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles          profiles         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  location_pings    location_pings[]

  @@index([organization_id, check_in_at(sort: Desc)], map: "attendance_sessions_org_check_in_idx")
  @@index([organization_id, user_id, check_in_at(sort: Desc)], map: "attendance_sessions_org_user_check_in_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model branches {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id String        @db.Uuid
  name            String
  code            String
  is_head_office  Boolean       @default(false)
  status          String        @default("active")
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  updated_at      DateTime      @default(now()) @db.Timestamptz(6)
  organizations   organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles        profiles[]

  @@unique([organization_id, code], map: "branches_code_unique")
  @@unique([organization_id, name], map: "branches_name_unique")
  @@index([organization_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model collection_entries {
  id                                               String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                                  String        @db.Uuid
  user_id                                          String        @db.Uuid
  party_id                                         String        @db.Uuid
  entry_date                                       DateTime      @db.Date
  amount                                           Decimal       @db.Decimal(18, 2)
  remarks                                          String        @default("")
  verification_status                              String        @default("unverified")
  created_by                                       String        @db.Uuid
  created_at                                       DateTime      @default(now()) @db.Timestamptz(6)
  updated_at                                       DateTime      @default(now()) @db.Timestamptz(6)
  profiles_collection_entries_created_byToprofiles profiles      @relation("collection_entries_created_byToprofiles", fields: [created_by], references: [id], onUpdate: NoAction)
  organizations                                    organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parties                                          parties       @relation(fields: [party_id], references: [id], onUpdate: NoAction)
  profiles_collection_entries_user_idToprofiles    profiles      @relation("collection_entries_user_idToprofiles", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([organization_id, entry_date(sort: Desc)], map: "collection_entries_org_entry_date_idx")
  @@index([organization_id, party_id], map: "collection_entries_org_party_idx")
  @@index([organization_id, user_id], map: "collection_entries_org_user_idx")
  @@index([organization_id, verification_status], map: "collection_entries_org_verification_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model collection_targets {
  id                                                        String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                                           String        @db.Uuid
  assigned_to_user_id                                       String        @db.Uuid
  party_id                                                  String?       @db.Uuid
  period_type                                               String
  start_date                                                DateTime      @db.Date
  end_date                                                  DateTime      @db.Date
  target_amount                                             Decimal       @db.Decimal(18, 2)
  status                                                    String        @default("draft")
  created_by                                                String        @db.Uuid
  created_at                                                DateTime      @default(now()) @db.Timestamptz(6)
  updated_at                                                DateTime      @default(now()) @db.Timestamptz(6)
  profiles_collection_targets_assigned_to_user_idToprofiles profiles      @relation("collection_targets_assigned_to_user_idToprofiles", fields: [assigned_to_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles_collection_targets_created_byToprofiles          profiles      @relation("collection_targets_created_byToprofiles", fields: [created_by], references: [id], onUpdate: NoAction)
  organizations                                             organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parties                                                   parties?      @relation(fields: [party_id], references: [id], onUpdate: NoAction)

  @@index([organization_id, assigned_to_user_id], map: "collection_targets_org_assignee_idx")
  @@index([organization_id, start_date, end_date], map: "collection_targets_org_dates_idx")
  @@index([organization_id, party_id], map: "collection_targets_org_party_idx", where: raw("(party_id IS NOT NULL)"))
  @@index([organization_id, status], map: "collection_targets_org_status_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model demand_order_dispatches {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id String        @db.Uuid
  demand_order_id String        @unique(map: "demand_order_dispatches_order_unique") @db.Uuid
  factory_status  String        @default("pending")
  challan_no      String?
  memo_no         String?
  dispatch_date   DateTime?     @db.Date
  remarks         String        @default("")
  updated_by      String?       @db.Uuid
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  updated_at      DateTime      @default(now()) @db.Timestamptz(6)
  demand_orders   demand_orders @relation(fields: [demand_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations   organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles        profiles?     @relation(fields: [updated_by], references: [id], onUpdate: NoAction)

  @@index([organization_id, factory_status], map: "demand_order_dispatches_org_status_idx")
  @@index([organization_id, updated_at(sort: Desc)], map: "demand_order_dispatches_org_updated_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model demand_order_items {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  demand_order_id String        @db.Uuid
  product_id      String        @db.Uuid
  quantity        Decimal       @db.Decimal(18, 4)
  unit_price      Decimal       @db.Decimal(18, 2)
  line_total      Decimal       @default(dbgenerated("round((quantity * unit_price), 2)")) @db.Decimal(18, 2)
  remark          String        @default("")
  demand_orders   demand_orders @relation(fields: [demand_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  products        products      @relation(fields: [product_id], references: [id], onUpdate: NoAction)

  @@index([demand_order_id], map: "demand_order_items_order_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model demand_orders {
  id                      String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id         String                   @db.Uuid
  party_id                String                   @db.Uuid
  created_by_user_id      String                   @db.Uuid
  order_date              DateTime                 @db.Date
  status                  String                   @default("draft")
  total_amount            Decimal                  @default(0) @db.Decimal(18, 2)
  remarks                 String                   @default("")
  submitted_at            DateTime?                @db.Timestamptz(6)
  created_at              DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at              DateTime                 @default(now()) @db.Timestamptz(6)
  stage                   String                   @default("draft")
  demand_order_dispatches demand_order_dispatches?
  demand_order_items      demand_order_items[]
  profiles                profiles                 @relation(fields: [created_by_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations           organizations            @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parties                 parties                  @relation(fields: [party_id], references: [id], onUpdate: NoAction)

  @@index([organization_id, created_by_user_id], map: "demand_orders_org_creator_idx")
  @@index([organization_id, order_date(sort: Desc)], map: "demand_orders_org_date_idx")
  @@index([organization_id, party_id], map: "demand_orders_org_party_idx")
  @@index([organization_id, stage, status], map: "demand_orders_org_stage_status_idx")
  @@index([organization_id, status], map: "demand_orders_org_status_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model location_pings {
  id                    String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id       String              @db.Uuid
  attendance_session_id String              @db.Uuid
  user_id               String              @db.Uuid
  captured_at           DateTime            @default(now()) @db.Timestamptz(6)
  lat                   Float
  lng                   Float
  accuracy              Float?
  speed                 Float?
  source                String              @default("web")
  created_at            DateTime            @default(now()) @db.Timestamptz(6)
  attendance_sessions   attendance_sessions @relation(fields: [attendance_session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations         organizations       @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles              profiles            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([organization_id, captured_at(sort: Desc)], map: "location_pings_org_captured_idx")
  @@index([attendance_session_id, captured_at(sort: Desc)], map: "location_pings_session_captured_idx")
  @@index([user_id, captured_at(sort: Desc)], map: "location_pings_user_captured_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model organizations {
  id                        String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                      String
  slug                      String                      @unique
  status                    String                      @default("active")
  created_at                DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  approval_logs             approval_logs[]
  attendance_sessions       attendance_sessions[]
  branches                  branches[]
  collection_entries        collection_entries[]
  collection_targets        collection_targets[]
  demand_order_dispatches   demand_order_dispatches[]
  demand_orders             demand_orders[]
  location_pings            location_pings[]
  parties                   parties[]
  products                  products[]
  profiles                  profiles[]
  roles                     roles[]
  sales_entries             sales_entries[]
  sales_targets             sales_targets[]
  visit_logs                visit_logs[]
  visit_plans               visit_plans[]
  work_plans                work_plans[]
  work_reports              work_reports[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model parties {
  id                                             String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                                String               @db.Uuid
  assigned_to_user_id                            String?              @db.Uuid
  name                                           String
  code                                           String?
  contact_person                                 String?
  phone                                          String?
  email                                          String?
  address                                        String?
  notes                                          String?
  status                                         String               @default("active")
  created_by_user_id                             String?              @db.Uuid
  created_at                                     DateTime             @default(now()) @db.Timestamptz(6)
  updated_at                                     DateTime             @default(now()) @db.Timestamptz(6)
  collection_entries                             collection_entries[]
  collection_targets                             collection_targets[]
  demand_orders                                  demand_orders[]
  profiles_parties_assigned_to_user_idToprofiles profiles?            @relation("parties_assigned_to_user_idToprofiles", fields: [assigned_to_user_id], references: [id], onUpdate: NoAction)
  profiles_parties_created_by_user_idToprofiles  profiles?            @relation("parties_created_by_user_idToprofiles", fields: [created_by_user_id], references: [id], onUpdate: NoAction)
  organizations                                  organizations        @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sales_entries                                  sales_entries[]
  sales_targets                                  sales_targets[]
  visit_logs                                     visit_logs[]
  visit_plans                                    visit_plans[]

  @@unique([organization_id, code], map: "parties_org_code_unique_idx", where: raw("(code IS NOT NULL)"))
  @@index([organization_id, assigned_to_user_id], map: "parties_org_assigned_idx")
  @@index([organization_id, name], map: "parties_org_name_idx")
  @@index([organization_id, status], map: "parties_org_status_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model products {
  id                 String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id    String               @db.Uuid
  product_name       String
  item_code          String
  unit               String
  base_price         Decimal              @default(0) @db.Decimal(12, 2)
  category           String?
  description        String?
  status             String               @default("active")
  created_by_user_id String?              @db.Uuid
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  demand_order_items demand_order_items[]
  profiles           profiles?            @relation(fields: [created_by_user_id], references: [id], onUpdate: NoAction)
  organizations      organizations        @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organization_id, item_code], map: "products_org_item_code_unique_idx")
  @@index([organization_id, category], map: "products_org_category_idx")
  @@index([organization_id, product_name], map: "products_org_name_idx")
  @@index([organization_id, status], map: "products_org_status_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model profiles {
  id                                                                  String                      @id @db.Uuid
  organization_id                                                     String?                     @db.Uuid
  branch_id                                                           String?                     @db.Uuid
  role_id                                                             String?                     @db.Uuid
  reports_to_user_id                                                  String?                     @db.Uuid
  full_name                                                           String?
  email                                                               String?
  phone                                                               String?
  employee_code                                                       String?
  designation                                                         String?
  avatar_url                                                          String?
  is_field_user                                                       Boolean                     @default(false)
  status                                                              String                      @default("invited")
  joined_at                                                           DateTime?                   @db.Timestamptz(6)
  created_at                                                          DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                                                          DateTime                    @default(now()) @db.Timestamptz(6)
  approval_logs_approval_logs_acted_by_user_idToprofiles              approval_logs[]             @relation("approval_logs_acted_by_user_idToprofiles")
  approval_logs_approval_logs_from_user_idToprofiles                  approval_logs[]             @relation("approval_logs_from_user_idToprofiles")
  approval_logs_approval_logs_to_user_idToprofiles                    approval_logs[]             @relation("approval_logs_to_user_idToprofiles")
  attendance_sessions                                                 attendance_sessions?
  collection_entries_collection_entries_created_byToprofiles          collection_entries[]        @relation("collection_entries_created_byToprofiles")
  collection_entries_collection_entries_user_idToprofiles             collection_entries[]        @relation("collection_entries_user_idToprofiles")
  collection_targets_collection_targets_assigned_to_user_idToprofiles collection_targets[]        @relation("collection_targets_assigned_to_user_idToprofiles")
  collection_targets_collection_targets_created_byToprofiles          collection_targets[]        @relation("collection_targets_created_byToprofiles")
  demand_order_dispatches                                             demand_order_dispatches[]
  demand_orders                                                       demand_orders[]
  location_pings                                                      location_pings[]
  parties_parties_assigned_to_user_idToprofiles                       parties[]                   @relation("parties_assigned_to_user_idToprofiles")
  parties_parties_created_by_user_idToprofiles                        parties[]                   @relation("parties_created_by_user_idToprofiles")
  products                                                            products[]
  branches                                                            branches?                   @relation(fields: [branch_id], references: [id], onUpdate: NoAction)
  users                                                               users                       @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  organizations                                                       organizations?              @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  profiles                                                            profiles?                   @relation("profilesToprofiles", fields: [reports_to_user_id], references: [id], onUpdate: NoAction)
  other_profiles                                                      profiles[]                  @relation("profilesToprofiles")
  roles                                                               roles?                      @relation(fields: [role_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  sales_entries_sales_entries_created_byToprofiles                    sales_entries[]             @relation("sales_entries_created_byToprofiles")
  sales_entries_sales_entries_user_idToprofiles                       sales_entries[]             @relation("sales_entries_user_idToprofiles")
  sales_targets_sales_targets_assigned_to_user_idToprofiles           sales_targets[]             @relation("sales_targets_assigned_to_user_idToprofiles")
  sales_targets_sales_targets_created_byToprofiles                    sales_targets[]             @relation("sales_targets_created_byToprofiles")
  visit_logs                                                          visit_logs[]
  visit_plans_visit_plans_created_byToprofiles                        visit_plans[]               @relation("visit_plans_created_byToprofiles")
  visit_plans_visit_plans_user_idToprofiles                           visit_plans[]               @relation("visit_plans_user_idToprofiles")
  work_plans_work_plans_owner_user_idToprofiles                       work_plans[]                @relation("work_plans_owner_user_idToprofiles")
  work_plans_work_plans_reviewed_byToprofiles                         work_plans[]                @relation("work_plans_reviewed_byToprofiles")
  work_reports_work_reports_owner_user_idToprofiles                   work_reports[]              @relation("work_reports_owner_user_idToprofiles")
  work_reports_work_reports_reviewed_byToprofiles                     work_reports[]              @relation("work_reports_reviewed_byToprofiles")

  @@unique([organization_id, employee_code], map: "profiles_org_employee_code_unique_idx", where: raw("(employee_code IS NOT NULL)"))
  @@index([branch_id])
  @@index([organization_id])
  @@index([reports_to_user_id])
  @@index([role_id])
  @@index([status])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model roles {
  id              String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id String?        @db.Uuid
  name            String
  slug            String         @unique(map: "roles_global_slug_unique_idx", where: raw("(organization_id IS NULL)"))
  level           Int
  description     String?
  is_system       Boolean        @default(false)
  status          String         @default("active")
  created_at      DateTime       @default(now()) @db.Timestamptz(6)
  updated_at      DateTime       @default(now()) @db.Timestamptz(6)
  profiles        profiles[]
  organizations   organizations? @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([organization_id, slug], map: "roles_org_slug_unique_idx", where: raw("(organization_id IS NOT NULL)"))
  @@index([organization_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sales_entries {
  id                                          String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                             String        @db.Uuid
  user_id                                     String        @db.Uuid
  party_id                                    String        @db.Uuid
  entry_date                                  DateTime      @db.Date
  amount                                      Decimal       @db.Decimal(18, 2)
  quantity                                    Decimal       @default(0) @db.Decimal(18, 4)
  remarks                                     String        @default("")
  source                                      String        @default("manual")
  created_by                                  String        @db.Uuid
  created_at                                  DateTime      @default(now()) @db.Timestamptz(6)
  updated_at                                  DateTime      @default(now()) @db.Timestamptz(6)
  profiles_sales_entries_created_byToprofiles profiles      @relation("sales_entries_created_byToprofiles", fields: [created_by], references: [id], onUpdate: NoAction)
  organizations                               organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parties                                     parties       @relation(fields: [party_id], references: [id], onUpdate: NoAction)
  profiles_sales_entries_user_idToprofiles    profiles      @relation("sales_entries_user_idToprofiles", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([organization_id, entry_date(sort: Desc)], map: "sales_entries_org_entry_date_idx")
  @@index([organization_id, party_id], map: "sales_entries_org_party_idx")
  @@index([organization_id, user_id], map: "sales_entries_org_user_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sales_targets {
  id                                                   String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                                      String        @db.Uuid
  assigned_to_user_id                                  String        @db.Uuid
  party_id                                             String?       @db.Uuid
  period_type                                          String
  start_date                                           DateTime      @db.Date
  end_date                                             DateTime      @db.Date
  target_amount                                        Decimal       @db.Decimal(18, 2)
  target_qty                                           Decimal?      @db.Decimal(18, 4)
  status                                               String        @default("draft")
  created_by                                           String        @db.Uuid
  created_at                                           DateTime      @default(now()) @db.Timestamptz(6)
  updated_at                                           DateTime      @default(now()) @db.Timestamptz(6)
  profiles_sales_targets_assigned_to_user_idToprofiles profiles      @relation("sales_targets_assigned_to_user_idToprofiles", fields: [assigned_to_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles_sales_targets_created_byToprofiles          profiles      @relation("sales_targets_created_byToprofiles", fields: [created_by], references: [id], onUpdate: NoAction)
  organizations                                        organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parties                                              parties?      @relation(fields: [party_id], references: [id], onUpdate: NoAction)

  @@index([organization_id, assigned_to_user_id], map: "sales_targets_org_assignee_idx")
  @@index([organization_id, start_date, end_date], map: "sales_targets_org_dates_idx")
  @@index([organization_id, party_id], map: "sales_targets_org_party_idx", where: raw("(party_id IS NOT NULL)"))
  @@index([organization_id, status], map: "sales_targets_org_status_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model visit_logs {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id String        @db.Uuid
  party_id        String        @db.Uuid
  user_id         String        @db.Uuid
  visit_plan_id   String?       @db.Uuid
  check_in_time   DateTime?     @db.Timestamptz(6)
  check_out_time  DateTime?     @db.Timestamptz(6)
  check_in_lat    Float?
  check_in_lng    Float?
  check_out_lat   Float?
  check_out_lng   Float?
  notes           String        @default("")
  outcome         String        @default("")
  status          String        @default("completed")
  created_at      DateTime      @default(now()) @db.Timestamptz(6)
  updated_at      DateTime      @default(now()) @db.Timestamptz(6)
  organizations   organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parties         parties       @relation(fields: [party_id], references: [id], onUpdate: NoAction)
  profiles        profiles      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  visit_plans     visit_plans?  @relation(fields: [visit_plan_id], references: [id], onUpdate: NoAction)

  @@index([organization_id, created_at(sort: Desc)], map: "visit_logs_org_created_at_idx")
  @@index([organization_id, party_id], map: "visit_logs_org_party_idx")
  @@index([organization_id, status], map: "visit_logs_org_status_idx")
  @@index([organization_id, user_id], map: "visit_logs_org_user_idx")
  @@index([visit_plan_id], map: "visit_logs_visit_plan_idx", where: raw("(visit_plan_id IS NOT NULL)"))
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model visit_plans {
  id                                        String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                           String        @db.Uuid
  party_id                                  String        @db.Uuid
  user_id                                   String        @db.Uuid
  visit_date                                DateTime      @db.Date
  purpose                                   String
  status                                    String        @default("planned")
  created_by                                String        @db.Uuid
  created_at                                DateTime      @default(now()) @db.Timestamptz(6)
  updated_at                                DateTime      @default(now()) @db.Timestamptz(6)
  visit_logs                                visit_logs[]
  profiles_visit_plans_created_byToprofiles profiles      @relation("visit_plans_created_byToprofiles", fields: [created_by], references: [id], onUpdate: NoAction)
  organizations                             organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parties                                   parties       @relation(fields: [party_id], references: [id], onUpdate: NoAction)
  profiles_visit_plans_user_idToprofiles    profiles      @relation("visit_plans_user_idToprofiles", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([organization_id, visit_date], map: "visit_plans_org_date_idx")
  @@index([organization_id, party_id], map: "visit_plans_org_party_idx")
  @@index([organization_id, status], map: "visit_plans_org_status_idx")
  @@index([organization_id, user_id], map: "visit_plans_org_user_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model work_plans {
  id                                          String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                             String        @db.Uuid
  owner_user_id                               String        @db.Uuid
  plan_date                                   DateTime      @db.Date
  title                                       String
  details                                     String
  priority                                    String?
  status                                      String        @default("draft")
  submitted_at                                DateTime?     @db.Timestamptz(6)
  reviewed_by                                 String?       @db.Uuid
  reviewed_at                                 DateTime?     @db.Timestamptz(6)
  review_note                                 String?
  created_at                                  DateTime      @default(now()) @db.Timestamptz(6)
  updated_at                                  DateTime      @default(now()) @db.Timestamptz(6)
  organizations                               organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles_work_plans_owner_user_idToprofiles profiles      @relation("work_plans_owner_user_idToprofiles", fields: [owner_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles_work_plans_reviewed_byToprofiles   profiles?     @relation("work_plans_reviewed_byToprofiles", fields: [reviewed_by], references: [id], onUpdate: NoAction)

  @@index([organization_id, plan_date], map: "work_plans_org_date_idx")
  @@index([organization_id, owner_user_id], map: "work_plans_org_owner_idx")
  @@index([organization_id, priority], map: "work_plans_org_priority_idx")
  @@index([organization_id, status], map: "work_plans_org_status_idx")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model work_reports {
  id                                            String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  organization_id                               String        @db.Uuid
  owner_user_id                                 String        @db.Uuid
  report_date                                   DateTime      @db.Date
  summary                                       String
  achievements                                  String?
  challenges                                    String?
  next_step                                     String?
  status                                        String        @default("draft")
  submitted_at                                  DateTime?     @db.Timestamptz(6)
  reviewed_by                                   String?       @db.Uuid
  reviewed_at                                   DateTime?     @db.Timestamptz(6)
  review_note                                   String?
  created_at                                    DateTime      @default(now()) @db.Timestamptz(6)
  updated_at                                    DateTime      @default(now()) @db.Timestamptz(6)
  organizations                                 organizations @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles_work_reports_owner_user_idToprofiles profiles      @relation("work_reports_owner_user_idToprofiles", fields: [owner_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles_work_reports_reviewed_byToprofiles   profiles?     @relation("work_reports_reviewed_byToprofiles", fields: [reviewed_by], references: [id], onUpdate: NoAction)

  @@index([organization_id, report_date], map: "work_reports_org_date_idx")
  @@index([organization_id, owner_user_id], map: "work_reports_org_owner_idx")
  @@index([organization_id, status], map: "work_reports_org_status_idx")
  @@schema("public")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}
